{
  "name": "@evryg/effect-integresql",
  "version": "0.0.1",
  "type": "module",
  "license": "MIT",
  "description": "An Effect based client of the integresql tool used for creating isolated postgresql databases used in integration tests",
  "homepage": "https://www.evryg.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/evryg-org/effect-contrib.git",
    "directory": "packages/integresql"
  },
  "bugs": {
    "url": "https://github.com/evryg-org/effect-contrib/issues"
  },
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts",
    "./internal/*": null
  },
  "publishConfig": {
    "access": "public",
    "directory": "dist",
    "linkDirectory": false,
    "provenance": true
  },
  "scripts": {
    "build": "pnpm build-esm && pnpm build-annotate && pnpm build-cjs && build-utils pack-v2 && cp ../../.npmignore dist/",
    "build-esm": "tsc -b tsconfig.build.json",
    "build-cjs": "babel build/esm --plugins @babel/transform-export-namespace-from --plugins @babel/transform-modules-commonjs --out-dir build/cjs --source-maps",
    "build-annotate": "babel build/esm --plugins annotate-pure-calls --out-dir build/esm --source-maps",
    "check": "tsc -b tsconfig.json",
    "test": "vitest",
    "coverage": "vitest --coverage"
  },
  "dependencies": {
    "fast-glob": "^3.3.2"
  },
  "peerDependencies": {
    "effect": "^3.21.0",
    "neo4j-driver": "^6.0.1"
  },
  "devDependencies": {
    "effect": "^3.21.0",
    "@testcontainers/postgresql": "^10.13.2",
    "testcontainers": "^10.13.2",
    "@effect/platform": "^0.94.4",
    "@effect/platform-node": "^0.104.1",
    "@effect/vitest": "^0.27.0",
    "@effect/sql-pg": "^0.50.3",
    "@effect/sql": "^0.49.0",
    "@types/pg": "^8.11.10",
    "vitest": "^3.2.4",
    "@types/node": "^22.7.5",
    "pg": "^8.13.0",
    "typescript": "^5.9.3"
  }
}
